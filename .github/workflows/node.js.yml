# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: CI PIPELINE

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
   # - run: npm test --passWithNoTests
    
      
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run:   
              echo "<dependency>" >> pom.xml
              echo "  <groupId>org.seleniumhq.selenium</groupId>" >> pom.xml
              echo "  <artifactId>selenium-java</artifactId>" >> pom.xml
              echo "  <version>4.5.3</version>" >> pom.xml
              echo "</dependency>" >> pom.xml
              echo "" >> pom.xml


      - name: Test code
        run: |
          # Add command to run the Selenium test script    
  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: success()
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and push Docker image
        run: |docker build . --Docker --l00171222/shibil_movie_course_website:$(date +%s)
             docker image push [OPTIONS] NAME[:TAG]
          # Use environment variables to access the repository credentials
